<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev-time local backend access (emulator + LAN phones). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
